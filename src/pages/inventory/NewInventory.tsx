import React from 'react'


const NewInventory = () => {
    //TODO: import the list of stores and products and create a form to add a new inventory for a product in a given store
    return (
    <div>
      Form to create a new Inventory
    </div>
  )
}

export default NewInventory
