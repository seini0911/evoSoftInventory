import React from 'react'
import {stores, employees, products} from '../../data/data';

const AdminDashboard = () => {
    console.log(stores, employees, products);
    //TODO: Display a dashboard with the number of stores, employees and products on separate cards and later a histogram of their inventories/stocks
    return (
    <div className='container px-4'>
        
    </div>
  )
}

export default AdminDashboard
